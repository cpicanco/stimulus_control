unit main;

{$MODE Delphi}

interface

uses
  LCLIntf, LCLType, LMessages, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs,
  StdCtrls;

type
  TForm1 = class(TForm)
    Button1: TButton;
    procedure Button1Click(Sender: TObject);
  private
    F : TTCPEchoDaemon;
  public
    { Public declarations }
  end;

var
  Form1: TForm1;

implementation

{$R main.lfm}

procedure TForm1.Button1Click(Sender: TObject);
begin
  F := TTCPEchoDaemon.create;
end;

end.